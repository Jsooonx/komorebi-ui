import type { ComponentPreviewProps } from "../../lib/components-manifest";
import Features5Element from "../blocks-elements/Features5Element";

export default function Features5Card({ minimal = false }: ComponentPreviewProps) {
  return <Features5Element minimal={minimal} />;
}
