import type { ComponentPreviewProps } from "../../lib/components-manifest";
import Features3Element from "../blocks-elements/Features3Element";

export default function Features3Card({ minimal = false }: ComponentPreviewProps) {
  return <Features3Element minimal={minimal} />;
}
