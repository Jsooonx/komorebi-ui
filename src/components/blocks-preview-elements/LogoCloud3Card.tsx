import type { ComponentPreviewProps } from "../../lib/components-manifest";
import LogoCloud3Element from "../blocks-elements/LogoCloud3Element";

export default function LogoCloud3Card({ minimal = false }: ComponentPreviewProps) {
  return <LogoCloud3Element minimal={minimal} />;
}
